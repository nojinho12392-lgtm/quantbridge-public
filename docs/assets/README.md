# Public Demo Assets

The images in this folder are public-facing demo assets for the repository
README and social preview.

They intentionally use synthetic company names, synthetic market labels, and
generic letter avatars. They do not include real company logos, brokerage data,
market-data cache files, or investment recommendations.

`demo-renderer.html` is a small local renderer used to regenerate the PNG files
with Chrome headless.
